defmodule CadenceCatalog do
  @moduledoc """
  Portable command and telemetry catalog models, importers, compilers, and
  codecs shared by Cadence and external peers.

  Persistence, tenancy, governance, activation, and runtime orchestration stay
  in the consuming applications.
  """
end
