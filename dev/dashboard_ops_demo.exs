# The observability exercise owns the full live contact, simulator, and
# persistence setup. This entrypoint names the broader product demo that it now
# seeds on top of that real data path.
Code.require_file("sre_observability_demo.exs", __DIR__)
